
<% if error.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(error.errors.count, "error") %> prohibited this from being saved:</h2>

    <ul>
      <% error.errors.full_messages.each do |error| %>
        <li><%= error %></li>
      <% end %>
    </ul>
  </div>
<% end %>