<h1><%= @album.name.upcase %> - <%=@album.musician.name%></h1>
<div>
<%= link_to 'Add a Song', new_album_song_path(@album) %> 
</div>
<br>
<% @album.songs.each do |s|%>
<div>
<%=link_to " #{s.title} ", album_song_path(@album, s) %>
</div>
<% end %>